-*- text -*-

- use Protocols to document the API of plugins, importers and
  exporters.

- The code duplicated between Store.Txo, Attribute.Txo and Schema.Txo is ugly

- ResultSet destruction and creation in BSDDB is slow.
